# Railway 會注入：DATABASE_URL, PORT, RAILWAY_PUBLIC_DOMAIN
DEBUG=False
# 部署後在 Railway → Variables 產生並設定：
# SECRET_KEY=<python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())">
