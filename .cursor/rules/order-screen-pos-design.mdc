---
description: 接單系統＝POS 收銀機；搜尋／接單／完成三頁準則（永久遵守）
alwaysApply: true
---

# 接單畫面設計準則（POS，非一般 ERP）

把接單當 **POS**：每做一個功能都要符合下列原則。違反 → 重新設計。

## 三頁分工（一頁一件事）

| 畫面 | 只做 | 唯一主要按鈕（綠色） |
|------|------|----------------------|
| 搜尋客戶 | 找客戶 | 【搜尋】 |
| 接單 | 選商品 | 【儲存訂單】 |
| 完成 | 告知下一步 | 【下一位客戶】 |

不要把管理、報表、設定、複雜選項塞進這三頁。

## 版面

- **iPad 橫向**：重要資訊**不必往下滑**才看得到；需一直滑 = 設計失敗。  
- **每屏只有一個「最重要」按鈕**；次要動作可見但視覺降級，不可搶主按鈕。

## 顏色語意（一致）

- **綠色** — 下一步／完成（主按鈕）  
- **紅色** — 刪除／作廢／放棄等危險  
- **灰色** — 不可操作  

全站同一意思同一顏色，不混用。

## 輸入與確認

- **不讓使用者思考** — 避免「你要不要…」「請選擇…」「是否…」；能直接做就做。  
- **不讓使用者輸入** — 能點不打、能選不輸入、能自動帶就不讓人選。  
- **危險操作必二次確認** — 刪單、作廢、清空、永久刪除等。  
- **錯誤說人話** — 禁 500/Exception/Validation；例：沒有商品、沒有網路、訂單尚未儲存，請重新操作。

## 回饋與導航

- 按鈕 **0.3 秒內**有回饋；不讓人懷疑有沒有按到。  
- **永遠可返回** — 一鍵回上一層，不迷路。

## 產品目標

不做「功能很多」的 ERP；做「**每天會想用**」的工具 — 只因更快、更簡單、更少出錯。  
新增功能前先問：增加**效率**還是**複雜度**？若是複雜度 → **不要加入**。

## 驗收

憲法驗收標準見 `yousin-erp-constitution.mdc`（70+ 首次獨立完成一張訂單）。
